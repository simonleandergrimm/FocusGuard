# Security policy

FocusGuard installs a root-owned helper and modifies a marked section of `/etc/hosts`.

The entire codebase was written by AI and has not been independently audited. Proceed with caution.
